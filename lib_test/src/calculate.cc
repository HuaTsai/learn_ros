#include <lib_test/calculate.h>

namespace lib_test {
  int Add(int a, int b) {
    return a + b;
  }

  int Minus(int a, int b) {
    return a - b;
  }

  int Multiply(int a, int b) {
    return a * b;
  }
}